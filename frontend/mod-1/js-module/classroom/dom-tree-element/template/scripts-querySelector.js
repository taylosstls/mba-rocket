// Acesse o elemento direto pelo seletor ID
const guest = document.querySelector('#guest-2');
console.log(guest)

// Retorna todos os elementos encontradas pelo query
const guests = document.querySelectorAll('.guest')
console.log(guests)